# Customer Workflow Templates

These are the reusable caller templates generated into customer repositories.
Each YAML file is paired with a `.properties.json` catalog entry and must pin
central reusable workflows and actions to the stable `@v1` release.

The backend consumes this directory from the central workflow snapshot. It
must not maintain a second copy of these templates.
